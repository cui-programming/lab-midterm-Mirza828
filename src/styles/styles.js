import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexGrow: 1,
    backgroundColor: '#b4d7ffff',
    alignItems: 'center',
    padding: 20,
  },

  teacherBox: {
    alignItems: 'center',
  },
  teacherTitle: {
    fontSize: 18,
    color: '#0740dbff',
  },
  teacherMsg: {
    textAlign: 'center',
    fontSize: 15,
  },

  tasbihContainer: {
    padding: 15,
    width: '100%',
  },

  searchContainer: {
    padding: 20,
    width: '100%',
  },
});
